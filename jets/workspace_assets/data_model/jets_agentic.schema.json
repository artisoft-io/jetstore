{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "jets_agentic.schema.json",
  "$comment": "jetstore-owned-asset: data_model/jets_agentic.schema.json \u2014 DO NOT EDIT. Source: `jets-agentic generate`, from tools/jets_agentic/jets_agentic/model.py. Installed into this workspace at image build time and overwritten on every build; a local edit is lost, and the build fails rather than clobbering it silently. Extend the model from your own file that imports this one.",
  "description": "The jets_agentic domain model, version 0.1.0. Decode against `jets-agentic schema` projections, not whole entities.",
  "$defs": {
    "AgentRun": {
      "additionalProperties": false,
      "description": "One execution of one agent: the unit the audit store correlates on\n(\u00a77.2's run_id) and the carrier of the reproducibility fields the\nproposal's generated_by block names (\u00a7A.2.7).",
      "properties": {
        "run_id": {
          "description": "The run identity; agent_audit.run_id correlates to it.",
          "type": "string"
        },
        "agent_id": {
          "description": "The agent that ran.",
          "type": "string"
        },
        "agent_version": {
          "description": "The agent's version, for reproducibility (\u00a7A.2.7 generated_by).",
          "type": "string"
        },
        "model_id": {
          "description": "The model that served the run.",
          "type": "string"
        },
        "prompt_version": {
          "description": "The prompt version, for reproducibility.",
          "type": "string"
        },
        "tier": {
          "$ref": "#/$defs/AutonomyTier",
          "description": "The autonomy tier the run executed under, recorded not enforced (\u00a79.1)."
        },
        "started_at": {
          "description": "When the run started.",
          "format": "date-time",
          "type": "string"
        },
        "ended_at": {
          "format": "date-time",
          "type": "string",
          "description": "When the run ended; unset while running."
        },
        "run_status": {
          "type": "string",
          "description": "The run outcome: succeeded, exhausted, interrupted or failed; unset while running (scoped per F7: status is \u00a7A.2.7's name on Incident). Corrected at D.5: this read 'succeeded, failed, interrupted' while the loop wrote 'exhausted' - the budget running out is not a failure, it is the compile-pass rate's denominator - and never wrote 'interrupted' until the same task gave it a wall-clock cap to distinguish from a caller's cancellation."
        },
        "triggered_by": {
          "type": "string",
          "description": "What started the run: a schedule, an anomaly, a user (scoped per F7: trigger_ref is \u00a7A.2.10's name on ChangeProposal)."
        },
        "domain_model_version": {
          "description": "The domain model version the run reasoned against (\u00a75.3).",
          "type": "string"
        },
        "iteration_cap": {
          "description": "Maximum propose-verify-repair iterations before the run ends as exhausted; the bound \u00a77's Rung 3 asks for without saying by what.",
          "type": "integer"
        },
        "wall_clock_cap_seconds": {
          "description": "Maximum wall-clock seconds for the whole run, carried on the run's context; the only cap that bounds a call the request timeout missed.",
          "type": "integer"
        },
        "token_spend": {
          "type": "integer",
          "description": "Total tokens consumed by the run, accumulated from the inference responses; unset while running. Recorded rather than capped in phase 1 - it is what lets the eval harness compare sampling policies at equal spend (\u00a74.3)."
        }
      },
      "required": [
        "run_id",
        "agent_id",
        "agent_version",
        "model_id",
        "prompt_version",
        "tier",
        "started_at",
        "domain_model_version",
        "iteration_cap",
        "wall_clock_cap_seconds"
      ],
      "type": "object"
    },
    "Anomaly": {
      "additionalProperties": false,
      "description": "A deterministic detector's observation (\u00a7A.2.6), widened at N.2 from the\nskeleton to what phase-3 plan \u00a712.2 says the execution record can actually\npopulate, and made a table.\n\n**The field list is derived from \u00a712.2's six derivable rows, not from\nreadability.** Every property below is populated by at least one of them;\ntwo of \u00a7A.2.6's optional properties are left out because none of the six can\npopulate them, which is \u00a712's own test applied honestly:\n\n- `confidence` \u2014 \u00a7A.2.6 has it optional. All six rows are predicates or\n  windowed aggregates over a relational table (\u00a712.3), so a detector emits a\n  fact and has no calibrated confidence to report. Adding it would invite a\n  constant. **Its consumer is the Observer agent, not a detector.**\n- `correlation_group_id` \u2014 \u00a7A.2.6 says *\"Assigned by Observer Agent\"*, so by\n  construction no deterministic detector writes it. Clustering anomalies\n  into an Incident is `Incident.hypotheses`' side of the model.\n\nBoth are recorded as I-126 rather than silently dropped.\n\n**Three of \u00a7A.2.6's required properties are optional here**, because \u00a712.2\nfixes which rows carry a number: `expected_min`, `expected_max` and\n`deviation_magnitude` exist for rows 1 and 3, which compare against a\nhistory window, and do not exist for rows 2, 4, 5 and 10, which are\nwithin-run predicates with no range and no magnitude. Requiring them would\nforce a detector to invent one. Recorded as I-126.\n\n**Names are class-scoped per F7 and the plan's instruction to name against\nthe namespace rather than for readability.** `subject_ref` is a hard\ncollision \u2014 it is ApprovalEvent's, declared there first \u2014 and `signal_type`,\n`observed_value`, `detector_ref` and `session_id` are exactly the generic\nnames a client workspace would want for something else. Each carries its\n\u00a7A.2.6 name in its description, per the model's documented-against rule.",
      "properties": {
        "anomaly_id": {
          "description": "The anomaly identity (\u00a7A.2.6: ^anom_[a-z0-9]+$).",
          "type": "string"
        },
        "detected_at": {
          "description": "When the detector emitted it.",
          "format": "date-time",
          "type": "string"
        },
        "anomaly_session_id": {
          "description": "The pipeline run the observation is drawn from - `session_id` on every table of the execution record, and the key `purge_database` deletes by (\u00a712.1). Scoped per F7, and deliberately not named run_ref: that is ApprovalEvent's and means an AgentRun, which this is not.",
          "type": "string"
        },
        "anomaly_subject_type": {
          "$ref": "#/$defs/AnomalySubject",
          "description": "What the observation is about (\u00a7A.2.6 names it subject_ref.entity_type). \u00a712.2's grain column: worker for rows 1-4, edge for rows 2, 5 and 10."
        },
        "anomaly_subject_ref": {
          "description": "The subject's identity (\u00a7A.2.6 names it subject_ref.entity_id; scoped per F7 against ApprovalEvent's subject_ref, which is a Remediation or ChangeProposal). For a worker, the step label, shard and jets_partition that key the row; for an edge, those plus the input and output channel.",
          "type": "string"
        },
        "anomaly_signal_type": {
          "$ref": "#/$defs/SignalType",
          "description": "Which failure mode was observed (\u00a7A.2.6 names it signal_type). Maps to a \u00a712.2 row: rejection_rate is row 1, volume is rows 2 and 5, step_regression row 3, worker_stall row 4, arrival row 10."
        },
        "anomaly_observed_value": {
          "description": "What was measured (\u00a7A.2.6 names it observed_value). **Text because \u00a7A.2.6 types it [number, string] and JetRules has no union**, and because \u00a712.2's rows genuinely differ: a ratio for row 1, a count for rows 2 and 5, a status for rows 3 and 4, a destination URI for row 10. The trigger property on ChangeProposal is carried as text for the same reason.",
          "type": "string"
        },
        "anomaly_expected_min": {
          "type": "string",
          "description": "The low end of what was expected (\u00a7A.2.6 names it expected_range.min; text on the same reasoning as observed_value). Optional rather than required as \u00a7A.2.6 has it: only rows 1 and 3 compare against a range at all."
        },
        "anomaly_expected_max": {
          "type": "string",
          "description": "The high end of what was expected (\u00a7A.2.6 names it expected_range.max). Optional, same reasoning as expected_min."
        },
        "anomaly_expected_basis": {
          "description": "**What the observation was compared against** (\u00a7A.2.6 names it expected_range.basis) - the history window and grouping for rows 1 and 3, the within-run invariant for rows 2, 4 and 5, the claim being checked for row 10. Required, and it is half of what \u00a712.3 says an actionable anomaly must be able to say.",
          "type": "string"
        },
        "anomaly_deviation_magnitude": {
          "type": "number",
          "description": "How far out, in standard deviations or as a ratio per signal type (\u00a7A.2.6 names it deviation_magnitude). Optional rather than required as \u00a7A.2.6 has it: rows 4, 5 and 10 are boolean predicates with no magnitude to report."
        },
        "anomaly_confounders": {
          "description": "**What the detector could not rule out** - the other half of \u00a712.3's test, and the per-observation form of \u00a712.2's *cannot see* column. Required; an empty list asserts that none of the fourteen applies, which is the contradicting_evidence discipline on Hypothesis reused. Has no \u00a7A.2.6 counterpart, because \u00a7A.2.6 was written against a warehouse rather than against this execution record.",
          "items": {
            "$ref": "#/$defs/AnomalyConfounder"
          },
          "type": "array"
        },
        "anomaly_detector_ref": {
          "description": "The deterministic detector that emitted this (\u00a7A.2.6 names it detector_ref, optional; required here because \u00a712.2's rows differ in what they can see and an anomaly whose detector is unknown cannot be read against the right row).",
          "type": "string"
        }
      },
      "required": [
        "anomaly_id",
        "detected_at",
        "anomaly_session_id",
        "anomaly_subject_type",
        "anomaly_subject_ref",
        "anomaly_signal_type",
        "anomaly_observed_value",
        "anomaly_expected_basis",
        "anomaly_confounders",
        "anomaly_detector_ref"
      ],
      "type": "object"
    },
    "AnomalyConfounder": {
      "description": "**Ours entirely, and it is the vocabulary form of \u00a712.2's *cannot see*\ncolumn** (14 values). Not in the proposal at any level.\n\nPhase-3 plan \u00a712.3 is the reason it exists: *\"every derivable row needs at\nleast one qualifier carried with the anomaly, or it is not actionable \u2026 an\n`Anomaly` that cannot say what it compared against and what it could not\nrule out reproduces the signal an operator learns to ignore\"*. `basis`\ncarries the first half; this carries the second.\n\n**A member is an alternative explanation the detector could not exclude, or\na bound on the comparison it made** \u2014 not a defect, and not a reason to\nsuppress the anomaly. Each is traceable to one cell of \u00a712.2 or to \u00a712.7,\nnamed in the comment beside it; **the list is appended to, never\nrenumbered**, on this repository's convention for numbered series.\n\nIt is deliberately *not* free text. A named `text` literal makes a mistyped\nvalue a compile error rather than a comparison that silently never matches\n(the .jr emitter's own argument), and free text would make two detectors'\nqualifiers incomparable, which defeats the purpose.",
      "enum": [
        "parse_errors_only",
        "parquet_input",
        "on_error_drop",
        "max_input_count",
        "sampling_cap",
        "device_writer_output",
        "merge_row_count_unknown",
        "step_label_ambiguous",
        "stall_cause_unknown",
        "cross_step_join_unavailable",
        "history_truncated",
        "no_physical_location",
        "stage_prefix_reused",
        "location_aimed_not_reached"
      ],
      "type": "string"
    },
    "AnomalySubject": {
      "description": "\u00a7A.2.6's `subject_ref.entity_type` \u2014 what an anomaly is *about*.\n\n**The first six are \u00a7A.2.6's; the last two are \u00a712.2's two grains.** The\nexecution record's rows are per *worker* (run \u00d7 step label \u00d7 shard \u00d7\n`jets_partition`) and per *edge* (worker \u00d7 input channel \u00d7 output channel),\nand neither is one of the six. `stage` is the near miss and is not the same\nthing: F52 reads `cpipes_step_id` as a stage location, but a worker is finer\nthan a stage by shard and partition, and an edge is finer again.\n\nAdded at N.2 with the SignalType additions above, on the same terms (I-127).",
      "enum": [
        "feed",
        "pipeline",
        "stage",
        "run",
        "table",
        "column",
        "worker",
        "edge"
      ],
      "type": "string"
    },
    "ApprovalEvent": {
      "additionalProperties": false,
      "description": "One human or agent decision on a supervised subject: the supervision\nseam. Outcomes are appended, never updated \u2014 a decision is a new event\nreferencing the same subject (\u00a77.2).",
      "properties": {
        "approval_event_id": {
          "description": "The event identity.",
          "type": "string"
        },
        "run_ref": {
          "description": "The AgentRun this event belongs to (scoped per F7: run_id names the run's own identity).",
          "type": "string"
        },
        "subject_ref": {
          "description": "The Remediation or ChangeProposal being decided.",
          "type": "string"
        },
        "from_state": {
          "$ref": "#/$defs/ApprovalState",
          "description": "The approval state before the decision."
        },
        "to_state": {
          "$ref": "#/$defs/ApprovalState",
          "description": "The approval state after the decision."
        },
        "actor": {
          "description": "Who decided: a user_email or an agent identity.",
          "type": "string"
        },
        "tier_at_event": {
          "$ref": "#/$defs/AutonomyTier",
          "description": "The autonomy tier at the time of the event (\u00a77.2; scoped per F7)."
        },
        "decided_at": {
          "description": "When the decision was made.",
          "format": "date-time",
          "type": "string"
        },
        "decision_rationale": {
          "type": "string",
          "description": "Why, in the actor's words (scoped per F7: rationale is \u00a7A.2.10's name on ChangeProposal)."
        }
      },
      "required": [
        "approval_event_id",
        "run_ref",
        "subject_ref",
        "from_state",
        "to_state",
        "actor",
        "tier_at_event",
        "decided_at"
      ],
      "type": "object"
    },
    "ApprovalState": {
      "description": "\u00a7A.4 \u2014 the approval lifecycle (9 values).",
      "enum": [
        "draft",
        "validated",
        "agent_reviewed",
        "awaiting_human_approval",
        "approved",
        "approved_with_modification",
        "rejected",
        "superseded",
        "deployed"
      ],
      "type": "string"
    },
    "AutonomyTier": {
      "description": "\u00a7A.4. Lexicographic order is tier order \u2014 the ceiling comparisons of\ngap 7 work on the text directly, so do not relabel to the \u00a79.2 names and\ndo not exceed T9 (\u00a74).",
      "enum": [
        "T0",
        "T1",
        "T2",
        "T3",
        "T4"
      ],
      "type": "string"
    },
    "ChangeProposal": {
      "additionalProperties": false,
      "description": "A proposed code or configuration change (\u00a7A.2.10), flattened to scalar\nand array properties: code_diff, ci_result and impact_analysis are nested\nobjects in the schema and JetRules has no anonymous nested object.",
      "properties": {
        "proposal_id": {
          "description": "The proposal identity (\u00a7A.2.10: ^chg_[a-z0-9]+$).",
          "type": "string"
        },
        "trigger": {
          "description": "What prompted the proposal (\u00a7A.4 ChangeTrigger vocabulary; carried as text in Phase 0 \u2014 the vocabulary ships with its consumer).",
          "type": "string"
        },
        "trigger_ref": {
          "type": "string",
          "description": "Incident, drift detection, or backlog item."
        },
        "affected_pipelines": {
          "description": "The pipelines the change touches (min 1).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "code_diff_repository": {
          "type": "string",
          "description": "code_diff.repository of \u00a7A.2.10."
        },
        "code_diff_branch": {
          "type": "string",
          "description": "code_diff.branch of \u00a7A.2.10."
        },
        "code_diff_files_changed": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "code_diff.files_changed of \u00a7A.2.10. Corrected at D.7: declared list[str] with default=None, which Pydantic does not validate and which the DDL emitter read as required-and-defaulted-to-null - two claims that cannot both hold. The default says the intent was optional."
        },
        "code_diff_lines_added": {
          "type": "integer",
          "description": "code_diff.lines_added of \u00a7A.2.10."
        },
        "code_diff_lines_removed": {
          "type": "integer",
          "description": "code_diff.lines_removed of \u00a7A.2.10."
        },
        "rationale": {
          "type": "string",
          "description": "Why the change is proposed."
        },
        "assumptions_made": {
          "items": {
            "type": "string"
          },
          "type": "array",
          "description": "The assumptions the proposal rests on. Corrected at D.7 with code_diff_files_changed, same defect."
        },
        "generated_tests": {
          "description": "The tests generated with the change (min 1).",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "ci_result_status": {
          "type": "string",
          "description": "ci_result.status: passed, failed, pending."
        },
        "ci_result_tests_run": {
          "type": "integer",
          "description": "ci_result.tests_run."
        },
        "ci_result_tests_failed": {
          "type": "integer",
          "description": "ci_result.tests_failed."
        },
        "impact_affected_assets": {
          "description": "impact_analysis.affected_downstream_assets, as asset refs.",
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "impact_clinical_relevance_touched": {
          "description": "impact_analysis.clinical_relevance_touched \u2014 the flag \u00a710 escalates on.",
          "type": "boolean"
        },
        "approval_state": {
          "$ref": "#/$defs/ApprovalState",
          "description": "Where the proposal sits in the approval lifecycle."
        },
        "proposal_model_version": {
          "description": "The domain model version the proposal was generated against (scoped per F7; domain_model_version stays on AgentRun).",
          "type": "string"
        }
      },
      "required": [
        "proposal_id",
        "trigger",
        "affected_pipelines",
        "generated_tests",
        "impact_affected_assets",
        "impact_clinical_relevance_touched",
        "approval_state",
        "proposal_model_version"
      ],
      "type": "object"
    },
    "DomainModelVersion": {
      "additionalProperties": false,
      "description": "A released version of the domain model itself (proposal \u00a75.3: identity\nand intent given, fields ours). Skeleton: the version and when it took\neffect.",
      "properties": {
        "version": {
          "description": "The semantic version of the model release.",
          "type": "string"
        },
        "effective_at": {
          "description": "When this version became the current one.",
          "format": "date-time",
          "type": "string"
        }
      },
      "required": [
        "version",
        "effective_at"
      ],
      "type": "object"
    },
    "EvalCase": {
      "additionalProperties": false,
      "description": "One evaluation case of the assurance suite (ours; \u00a711's consumer\narrives in a later phase). Skeleton: identity only.",
      "properties": {
        "eval_case_id": {
          "description": "The evaluation case identity.",
          "type": "string"
        }
      },
      "required": [
        "eval_case_id"
      ],
      "type": "object"
    },
    "Evidence": {
      "additionalProperties": false,
      "description": "One evidence item of a Hypothesis (\u00a7A.2.8 `$defs`): a value object in\nworking memory, so \u00a7B.3's escalation trigger \u2014 contradictory evidence\nexceeding supporting \u2014 is a rule-countable fact rather than a JSON blob.",
      "properties": {
        "statement": {
          "description": "The evidence, in words.",
          "type": "string"
        },
        "source": {
          "$ref": "#/$defs/EvidenceSource",
          "description": "Where the evidence came from."
        },
        "source_ref": {
          "type": "string",
          "description": "A resolvable reference into the source."
        }
      },
      "required": [
        "statement",
        "source"
      ],
      "type": "object"
    },
    "EvidenceSource": {
      "description": "\u00a7A.2.8's `$defs` \u2014 scoped to Evidence and deliberately NOT in \u00a7A.4,\nwhich is why the emitters discover vocabularies by reachability (9 values).",
      "enum": [
        "run_telemetry",
        "lineage",
        "commit_history",
        "prior_incident",
        "infrastructure_log",
        "source_delivery_history",
        "dq_result",
        "code_inspection",
        "profile"
      ],
      "type": "string"
    },
    "Hypothesis": {
      "additionalProperties": false,
      "description": "A causal hypothesis for an Incident (\u00a7A.2.8): the RCA agent's output\ncontract (\u00a7B.3). contradicting_evidence is required \u2014 a calibration\ncontrol, not a documentation nicety (\u00a7A.2.8's own note).",
      "properties": {
        "hypothesis_id": {
          "description": "The hypothesis identity.",
          "type": "string"
        },
        "cause": {
          "description": "The hypothesised cause (max 500 chars in \u00a7A.2.8).",
          "type": "string"
        },
        "cause_category": {
          "$ref": "#/$defs/IncidentClassification",
          "description": "The causal taxonomy entry."
        },
        "confidence": {
          "description": "The agent's confidence, 0..1.",
          "type": "number"
        },
        "rank": {
          "description": "The rank among the incident's hypotheses, 1-based.",
          "type": "integer"
        },
        "supporting_evidence": {
          "description": "The evidence for (min 1).",
          "items": {
            "$ref": "#/$defs/Evidence"
          },
          "type": "array"
        },
        "contradicting_evidence": {
          "description": "The evidence against \u2014 required; empty only where the agent explicitly asserts none exists (\u00a7A.2.8).",
          "items": {
            "$ref": "#/$defs/Evidence"
          },
          "type": "array"
        }
      },
      "required": [
        "hypothesis_id",
        "cause",
        "confidence",
        "rank",
        "supporting_evidence",
        "contradicting_evidence"
      ],
      "type": "object"
    },
    "Incident": {
      "additionalProperties": false,
      "description": "A triaged cluster of anomalies (\u00a7A.2.7). Skeleton: identity, lifecycle\nand the classification trio; the impact and generated_by fields arrive\nwith their consumer.",
      "properties": {
        "incident_id": {
          "description": "The incident identity (\u00a7A.2.7: ^inc_[a-z0-9]+$).",
          "type": "string"
        },
        "classification": {
          "$ref": "#/$defs/IncidentClassification",
          "description": "The causal classification."
        },
        "severity": {
          "$ref": "#/$defs/Severity",
          "description": "The severity."
        },
        "status": {
          "$ref": "#/$defs/IncidentStatus",
          "description": "Where the incident sits in \u00a7A.5's state machine."
        },
        "hypotheses": {
          "description": "The ranked causal hypotheses.",
          "items": {
            "$ref": "#/$defs/Hypothesis"
          },
          "type": "array"
        },
        "incident_model_version": {
          "description": "\u00a7A.2.7's domain_model_version, scoped per F7: the model version the incident was raised against.",
          "type": "string"
        }
      },
      "required": [
        "incident_id",
        "classification",
        "severity",
        "status",
        "incident_model_version"
      ],
      "type": "object"
    },
    "IncidentClassification": {
      "description": "\u00a7A.4 \u2014 the causal taxonomy of an incident (10 values).",
      "enum": [
        "source_delivery_failure",
        "source_content_change",
        "transport_failure",
        "parse_failure",
        "validation_breach",
        "transformation_defect",
        "infrastructure_failure",
        "dependency_failure",
        "capacity_or_cost_deviation",
        "benign_variation"
      ],
      "type": "string"
    },
    "IncidentStatus": {
      "description": "\u00a7A.4 \u2014 the incident lifecycle (11 values); the state machine is \u00a7A.5's.",
      "enum": [
        "detected",
        "triaged",
        "diagnosed",
        "remediation_proposed",
        "awaiting_approval",
        "remediating",
        "resolved",
        "verified",
        "closed",
        "reclassified",
        "suppressed_as_benign"
      ],
      "type": "string"
    },
    "Remediation": {
      "additionalProperties": false,
      "description": "A proposed corrective action (\u00a7A.2.9). Skeleton: identity, the incident\nit corrects, and the two governing states.",
      "properties": {
        "remediation_id": {
          "description": "The remediation identity (\u00a7A.2.9: ^rem_[a-z0-9]+$).",
          "type": "string"
        },
        "incident_ref": {
          "description": "The Incident this remediates (\u00a7A.2.9 names it incident_id; scoped per F7 against Incident's identity).",
          "type": "string"
        },
        "autonomy_tier_required": {
          "$ref": "#/$defs/AutonomyTier",
          "description": "The tier the action requires (recorded; enforcement is by privilege, \u00a79.1)."
        },
        "remediation_approval_state": {
          "$ref": "#/$defs/ApprovalState",
          "description": "Where the remediation sits in the approval lifecycle (\u00a7A.2.9 names it approval_state; scoped per F7 against ChangeProposal's)."
        }
      },
      "required": [
        "remediation_id",
        "incident_ref",
        "autonomy_tier_required",
        "remediation_approval_state"
      ],
      "type": "object"
    },
    "Severity": {
      "description": "\u00a7A.4 (5 values).",
      "enum": [
        "info",
        "low",
        "medium",
        "high",
        "critical"
      ],
      "type": "string"
    },
    "SignalType": {
      "description": "\u00a7A.2.6's `$defs` \u2014 scoped to Anomaly, like EvidenceSource not in \u00a7A.4.\n\n**The first ten are \u00a7A.2.6's, verbatim and in its order. The last three are\nours**, added at N.2 because the ten do not name three of the six failure\nmodes phase-3 plan \u00a712.2 found derivable from JetStore's execution record,\nand `signal_type` is required by \u00a7A.2.6 \u2014 so without them an anomaly for\nthose rows is unrepresentable rather than merely unlabelled.\n\nThe ten were written for a warehouse's data-quality signals (volume,\nfreshness, distribution, cardinality); \u00a712.2's rows are *execution* signals,\nwhich is a different substrate rather than a gap in the proposal. Of the\nten, \u00a712.2 populates exactly two today: `rejection_rate` is row 1 and\n`volume` is rows 2 and 5.\n\n**N.4 is the party that confirms or overturns the three additions** \u2014 it\nbuilds the first two detectors and is the first caller that has to choose a\nvalue. Raised as I-127.",
      "enum": [
        "volume",
        "freshness",
        "schema",
        "distribution",
        "rule_breach",
        "cost",
        "duration",
        "rejection_rate",
        "cardinality",
        "referential",
        "step_regression",
        "worker_stall",
        "arrival"
      ],
      "type": "string"
    }
  }
}
