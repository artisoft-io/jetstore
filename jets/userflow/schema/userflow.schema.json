{
  "$comment": "jetstore-owned-asset — DO NOT EDIT. Source: ../../jetsclient_ide/src/userflow/userflow.schema.json, emitted by `npm run emit-schema` in jetsclient_ide. This copy exists because //go:embed cannot reach outside its own directory; TestSchemaCopiesMatchSource fails if the two drift, so a local edit is caught rather than silently overwritten.",
  "$defs": {
    "Choice": {
      "additionalProperties": false,
      "description": "A guarded transition to another state",
      "properties": {
        "nextState": {
          "description": "A key naming a state, form, action or form-state entry",
          "minLength": 1,
          "pattern": "^[A-Za-z0-9_.-]+$",
          "type": "string"
        },
        "when": {
          "$ref": "#/$defs/Condition"
        }
      },
      "required": [
        "when",
        "nextState"
      ],
      "type": "object"
    },
    "Condition": {
      "anyOf": [
        {
          "additionalProperties": false,
          "properties": {
            "key": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "op": {
              "const": "equals",
              "type": "string"
            },
            "value": {
              "type": "string"
            }
          },
          "required": [
            "op",
            "key",
            "value"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "key": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "op": {
              "const": "equals",
              "type": "string"
            },
            "valueFromKey": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            }
          },
          "required": [
            "op",
            "key",
            "valueFromKey"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "key": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "op": {
              "const": "contains",
              "type": "string"
            },
            "value": {
              "type": "string"
            }
          },
          "required": [
            "op",
            "key",
            "value"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "key": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "op": {
              "const": "contains",
              "type": "string"
            },
            "valueFromKey": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            }
          },
          "required": [
            "op",
            "key",
            "valueFromKey"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "key": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "op": {
              "const": "isNull",
              "type": "string"
            }
          },
          "required": [
            "op",
            "key"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "key": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "op": {
              "const": "isNullOrEmpty",
              "type": "string"
            }
          },
          "required": [
            "op",
            "key"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "condition": {
              "$ref": "#/$defs/Condition"
            },
            "op": {
              "const": "not",
              "type": "string"
            }
          },
          "required": [
            "op",
            "condition"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "conditions": {
              "items": {
                "$ref": "#/$defs/Condition"
              },
              "minItems": 1,
              "type": "array"
            },
            "op": {
              "const": "and",
              "type": "string"
            }
          },
          "required": [
            "op",
            "conditions"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "conditions": {
              "items": {
                "$ref": "#/$defs/Condition"
              },
              "minItems": 1,
              "type": "array"
            },
            "op": {
              "const": "or",
              "type": "string"
            }
          },
          "required": [
            "op",
            "conditions"
          ],
          "type": "object"
        }
      ],
      "description": "A predicate over form state, guarding a transition or a step"
    },
    "State": {
      "anyOf": [
        {
          "additionalProperties": false,
          "properties": {
            "description": {
              "minLength": 1,
              "type": "string"
            },
            "formConfig": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "goToStates": {
              "items": {
                "description": "A key naming a state, form, action or form-state entry",
                "minLength": 1,
                "pattern": "^[A-Za-z0-9_.-]+$",
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            "isEnd": {
              "const": true,
              "type": "boolean"
            },
            "stateAction": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            }
          },
          "required": [
            "description",
            "formConfig",
            "isEnd"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "choices": {
              "items": {
                "$ref": "#/$defs/Choice"
              },
              "minItems": 1,
              "type": "array"
            },
            "defaultNextState": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "description": {
              "minLength": 1,
              "type": "string"
            },
            "formConfig": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "goToStates": {
              "items": {
                "description": "A key naming a state, form, action or form-state entry",
                "minLength": 1,
                "pattern": "^[A-Za-z0-9_.-]+$",
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            "isEnd": {
              "const": false,
              "type": "boolean"
            },
            "stateAction": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            }
          },
          "required": [
            "description",
            "formConfig",
            "choices"
          ],
          "type": "object"
        },
        {
          "additionalProperties": false,
          "properties": {
            "choices": {
              "items": {
                "$ref": "#/$defs/Choice"
              },
              "minItems": 1,
              "type": "array"
            },
            "defaultNextState": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "description": {
              "minLength": 1,
              "type": "string"
            },
            "formConfig": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            },
            "goToStates": {
              "items": {
                "description": "A key naming a state, form, action or form-state entry",
                "minLength": 1,
                "pattern": "^[A-Za-z0-9_.-]+$",
                "type": "string"
              },
              "minItems": 1,
              "type": "array"
            },
            "isEnd": {
              "const": false,
              "type": "boolean"
            },
            "stateAction": {
              "description": "A key naming a state, form, action or form-state entry",
              "minLength": 1,
              "pattern": "^[A-Za-z0-9_.-]+$",
              "type": "string"
            }
          },
          "required": [
            "description",
            "formConfig",
            "defaultNextState"
          ],
          "type": "object"
        }
      ],
      "description": "One step of the flow: a form, an optional action, and where to go next"
    }
  },
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "additionalProperties": false,
  "description": "A state machine over screens, authored as data and interpreted at run time",
  "properties": {
    "exitScreenPath": {
      "minLength": 1,
      "type": "string"
    },
    "formStateInitializer": {
      "description": "A key naming a state, form, action or form-state entry",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9_.-]+$",
      "type": "string"
    },
    "schemaVersion": {
      "const": 1,
      "type": "number"
    },
    "startAtKey": {
      "description": "A key naming a state, form, action or form-state entry",
      "minLength": 1,
      "pattern": "^[A-Za-z0-9_.-]+$",
      "type": "string"
    },
    "states": {
      "additionalProperties": {
        "$ref": "#/$defs/State"
      },
      "propertyNames": {
        "description": "A key naming a state, form, action or form-state entry",
        "minLength": 1,
        "pattern": "^[A-Za-z0-9_.-]+$",
        "type": "string"
      },
      "type": "object"
    },
    "title": {
      "minLength": 1,
      "type": "string"
    }
  },
  "required": [
    "schemaVersion",
    "startAtKey",
    "states"
  ],
  "title": "JetStore UserFlow configuration",
  "type": "object"
}
